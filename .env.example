# Example environment variables for Analytics Copilot (Text-to-SQL)

# Hugging Face access token (if needed for private models or higher rate limits)
# Obtain from: https://huggingface.co/settings/tokens
HF_TOKEN=your_hf_token_here

# Optional: default Hugging Face cache directory override
# HF_HOME=/path/to/hf/home

# Optional: default dataset cache directory
# HF_DATASETS_CACHE=/path/to/hf/datasets/cache

# Optional: default model cache directory
# HF_HOME=/path/to/hf/models/cache

# Database connection string for demo (e.g., local SQLite or Postgres)
# DB_URL=sqlite:///./data/demo.db