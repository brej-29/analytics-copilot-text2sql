# Example Streamlit secrets for the Analytics Copilot Text-to-SQL demo.
#
# IMPORTANT:
# - Do NOT commit real tokens or endpoint URLs to version control.
# - Create a local `.streamlit/secrets.toml` (ignored by git) and fill in
#   environment-specific values there, or configure secrets directly in
#   Streamlit Community Cloud.

HF_TOKEN = "hf_your_access_token_here"

# Either:
#   1) Point to a dedicated Inference Endpoint / TGI deployment via base_url, or
#   2) Use a model id served by Hugging Face Inference / a provider.
HF_MODEL_ID = "brej-29/analytics-copilot-text2sql-mistral7b-qlora"

# Optional: dedicated inference endpoint base URL
HF_INFERENCE_BASE_URL = ""  # e.g. "https://your-endpoint-url"

# Optional: provider hint for InferenceClient when using HF_MODEL_ID.
# Common values: "auto", "hf-inference", "tgi".
HF_PROVIDER = "auto"